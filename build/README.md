# Build resources

Drop platform icons here for `electron-builder` to pick up automatically:

- `icon.ico` — Windows app/installer icon (256×256 or larger recommended).
- `icon.png` — Linux app icon (512×512 or larger recommended).

If no icon is present, electron-builder falls back to the default Electron
icon and packaging still succeeds.
